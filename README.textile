h1. Team Retrospective

h2. Prerequisites
# $>brew install redis
# $>brew install git

Also, you need to install rvm via 'bash < <(curl -s https://rvm.beginrescueend.com/install/rvm)'

h2. To run the application
# $>bundle install
# $>rake redis:start
# $>rake server:start
